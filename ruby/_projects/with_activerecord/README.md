```
bundle
bundle exec rake --tasks
rake --tasks
bundle exec rake db:create # Эта команда фэйлится ыыы, ясное дело, создаём вручную
bundle exec rake db:new_migration[create_movies]
bundle exec rake db:migrate
chmod u+x app/main.rb
bundle exec ruby app/main.rb
./app/main.rb
```
