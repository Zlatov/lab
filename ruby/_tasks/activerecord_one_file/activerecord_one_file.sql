-- DROP TABLE IF EXISTS posts;
-- CREATE TABLE posts (
--   id SERIAL PRIMARY KEY,
--   header CHAR(255) NOT NULL UNIQUE,
--   slug CHAR(255) NOT NULL UNIQUE,
--   content TEXT,
--   title CHAR(255) UNIQUE,
--   description CHAR(255),
--   keywords CHAR(255)
-- );

SELECT * FROM posts;
